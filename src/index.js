import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';

// root creation
const root = ReactDOM.createRoot(document.getElementById("root"))

// now we need to inject root component hence

root.render(<App />);


